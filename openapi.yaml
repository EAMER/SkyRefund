openapi: 3.0.3
info:
  title: SkyRefundSage API
  version: 1.0.0
  description: Basic API contract for the SkyRefundSage refund management platform.
servers:
  - url: http://localhost:8000/api/v1
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
